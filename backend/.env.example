# App runtime user — least-privilege role (SELECT/INSERT/UPDATE/DELETE only)
DATABASE_URL=postgresql://sms_app:change-me@localhost:5432/sms_db

# Migration user — superuser or DDL-capable role (CREATE/DROP/ALTER).
# Omit in development; Alembic will fall back to DATABASE_URL.
# MIGRATION_DATABASE_URL=postgresql://sms_migrations:change-me@localhost:5432/sms_db

SECRET_KEY=change-this-to-a-random-secret-key-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
